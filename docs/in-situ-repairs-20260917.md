# In-place app corrections — 17 September 2026

Ten existing app pages are corrected at their original URLs. The catalogue retains their current homes, removes its duplicate climate-path entry and describes the corrected behaviour. No app is migrated or deleted.

- The four decision pages distinguish executable models, worked diagrams, historical chart data and framing worksheets. The live sequential model now implements five declared policy representatives, separates fixed latent effectiveness from belief, uses common shocks and computes its benchmark from current controls. The questionnaire includes the missing transition model. The historical worksheet loads real newlines. [Model details and limits](decision-repairs-method.md).
- The grounded-theory page replaces random grouping and an unconditional Core with chosen incident identifications, a computed finite quotient and a compatible-coding factor-map check. It states the exact singleton-set diagram and the limits of the qualitative analogy. It makes no initial-algebra or saturation claim.
- The two control-centre architecture pages replace unsupported clearances, permissions, verified-design claims and closed-risk claims with concept hypotheses and open interface questions. The three other edited geometry pages clarify assumed bands and levels. The topology page's ten sliders now update model state before rendering, fixing a one-event lag in the diagram and exports. All architecture alternatives, layers, section cuts and exports remain.

## Verification

`npm test` runs the model and regression suites and also runs before Pages deployment:

- 19 decision checks cover repeatability, non-anticipation, Bayesian updates, zero-exposure learning, declared policy objectives, budget/world updates, bounded inputs, computed benchmarks, quantiles, preserved Plotly payloads and framing output.
- 76 finite identification graphs and 1,099 candidate coding maps are independently checked, including the returned factor map and conflict witness.
- 30 slider events across ten inputs check state and label precede exactly one redraw; the test fails on the original stale-render bug.

Local browser journeys covered simulator recalculation and invalid-input recovery; generated framing outputs; worked-path navigation; historical charts; the coding conflict/compatible/undo/new-incident loops; architecture tabs/register/gallery return; layer/section controls and current-value SVG export. Desktop and phone layouts were inspected for the substantially changed pages. These are automated/agent-operated checks, not human-user validation or engineering verification.
