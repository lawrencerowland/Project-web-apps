digraph G {
  // Keep only edges that affect target T
  A -> B; B -> C; C -> T;
  A -> D; D -> E; // should be pruned if E !-> T
  X -> Y;         // isolated component, prune
  // cosmetic attrs:
  node [shape=box];
}
